<div class="mb-4">
  <label for="categ-select" class="form-label fw-semibold">Categoría</label>
  <select name="categSelect" id="selectForm-add" class="form-select">
    <option selected disabled>Selecciona una opción</option>
    {{#each categories}}
    <option value="{{id}}">{{name}}</option>
    {{/each}}
  </select>
</div>